import { ClassType, ComponentType } from 'react';
import { useParams } from 'react-router-dom';

/* export function withParams(Component: ComponentType) {
  return (props: any) => <Component {...props} params={useParams()} />;
} */
